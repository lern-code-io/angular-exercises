import {Directive, ElementRef} from '@angular/core';

@Directive({
  selector: '[appBlueBackground]',
  standalone: true
})
export class BlueBackgroundDirective {

  // grab the element via the constructor and change background color here

}
